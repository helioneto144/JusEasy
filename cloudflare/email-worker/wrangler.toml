name = "juseasy-email-worker"
main = "src/index.js"
compatibility_date = "2026-04-28"

# Configurar via dashboard ou wrangler secret put:
#   wrangler secret put WEBHOOK_URL
#   wrangler secret put WEBHOOK_SECRET
#
# Email Routing: bind este worker via Dashboard
#   Email → Email Routing → Email Workers → Selecionar este worker
